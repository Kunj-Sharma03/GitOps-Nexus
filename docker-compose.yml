# Docker Compose - runs Redis for job queue
# 
# To start: docker-compose up -d
# To stop:  docker-compose down

version: '3.8'

services:
  # Redis - for job queue
  redis:
    image: redis:7-alpine
    container_name: gitops-redis
    ports:
      - "6379:6379"
    restart: unless-stopped


